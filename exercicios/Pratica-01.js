// Primeira Operação

const soma = 6 + 6;

const multiplicacao = 5*5

const operacao = (soma + multiplicacao) + 15;

console.log(operacao);


// Segunda operação

const somaDois = 10 + 5 + 8;

const divisao = 100/10;

const multiplicacaoDois = (5 *5) + 2 * 8;

const subtracao = 100 - 80;


const operacaoDois = (somaDois * divisao) - multiplicacaoDois + subtracao;

console.log(operacaoDois);